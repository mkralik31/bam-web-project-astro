---
import { Keystatic } from '@keystatic/core/ui';
import config from '../../../keystatic.config';

export function getStaticPaths() {
  return [{ params: { file: undefined } }];
}
---

<Keystatic config={config as any} client:only="react" />