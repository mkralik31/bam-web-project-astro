<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

# Ak súbor ani priečinok neexistujú, presmeruj na Keystatic SPA

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^keystatic(.\*)$ keystatic/index.html [L]
</IfModule>
